import React from "react";
import {

  render,
 

} from "@testing-library/react";
import ModalAddHash from "../ModalAddHash";

// Unit tests
describe("Basic tests for component", () => {
  test("Basic example", async () => {
    render(<ModalAddHash />);
    
 
  });
});